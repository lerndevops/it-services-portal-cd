apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: it-services-portal
  namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml

commonLabels:
  app: it-services-portal
