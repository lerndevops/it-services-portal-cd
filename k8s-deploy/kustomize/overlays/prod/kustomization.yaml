apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: it-services-portal-prod
  namespace: prod

bases:
  - ../../base

commonLabels:
  env: production

patchesStrategicMerge:
  - deployment-patch.yaml
  - service-patch.yaml
  - configmap-patch.yaml
