apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: it-services-portal-uat
  namespace: uat

bases:
  - ../../base

commonLabels:
  env: uat

patchesStrategicMerge:
  - deployment-patch.yaml
  - service-patch.yaml
  - configmap-patch.yaml
